{% extends 'base.html.twig' %}

{% block title %}Secretary{% endblock %}

{% block body %}
    <h1>Secretary</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ secretary.id }}</td>
            </tr>
            <tr>
                <th>Genre</th>
                <td>{{ secretary.genre }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ secretary.name }}</td>
            </tr>
            <tr>
                <th>Firstname</th>
                <td>{{ secretary.firstname }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ secretary.adresse }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ secretary.phone }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ secretary.email }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_secretary_index') }}">back to list</a>

    <a href="{{ path('app_secretary_edit', {'id': secretary.id}) }}">edit</a>

    {{ include('secretary/_delete_form.html.twig') }}
{% endblock %}
